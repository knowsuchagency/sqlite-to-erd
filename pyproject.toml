[project]
name = "sqlite-to-erd"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.2.1",
]

[project.scripts]
sqlite-to-erd = "sqlite_to_erd:main"
